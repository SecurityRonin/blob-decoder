# blob-decoder

Identify and decode opaque forensic blobs of unknown type — scored, cited
candidates, recursively unwrapping nested wrappers.

Scaffold in progress. See the `feat/blob-decoder` branch for the engine.
