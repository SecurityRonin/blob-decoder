//! `blob-decode` CLI — a thin Humble-Object shell over the `blob-decoder` engine.
#![forbid(unsafe_code)]

fn main() {
    // Scaffold placeholder; the CLI is implemented on the feat/blob-decoder branch.
}
